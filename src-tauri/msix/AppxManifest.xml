<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest for the Microsoft Store, wrapping the self-contained Tauri exe as a
  packaged full-trust desktop app.

  IMPORTANT: Name/Publisher below are PLACEHOLDERS. Register this app as a new listing
  in Partner Center (App management -> App identity) and Partner Center will hand you
  the real Package/Identity Name and Publisher CN to use here - they cannot be reused
  from the Piano Teacher listing, each Store app needs its own identity.
  pack.ps1 stages the exe + icons next to a copy of this file and runs MakeAppx.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <!-- Version is stamped from package.json by pack.ps1 at pack time; this literal is
       just a fallback/placeholder, keep it roughly in sync to avoid confusion. -->
  <Identity
    Name="Callum-OP.Animare3DAnimator"
    Publisher="CN=08FEADE4-7024-438F-99AB-86942FD832C3"
    Version="0.1.0.0"
    ProcessorArchitecture="arm64" />

  <Properties>
    <DisplayName>Animare 3D Animator</DisplayName>
    <PublisherDisplayName>Callum-OP</PublisherDisplayName>
    <Logo>icons/StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="Animare3DAnimator" Executable="animare-3d-animator.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Animare 3D Animator"
        Description="A lightweight, browser-based tool for posing, animating, and rasterizing 3D characters exported from Blender."
        BackgroundColor="#202020"
        Square150x150Logo="icons\Square150x150Logo.png"
        Square44x44Logo="icons\Square44x44Logo.png">
        <uap:DefaultTile Square71x71Logo="icons\Square71x71Logo.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
